syntax = "proto3";

package order;

service OrderService {
    rpc uploadTracking (Request) returns (Response);
}

message Request {
    string jsonData = 1;
}

message Response {
    string message = 1;
}
